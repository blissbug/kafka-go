// Package broker is the top-level object that owns all topics and
// handles incoming produce/consume calls from the network layer,
// routing them to the right topic/partition.
//
// Not implemented yet - depends on topic and partition existing first.
package broker
